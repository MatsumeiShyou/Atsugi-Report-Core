# 厚木事業所_入荷日報自動化_AGENTS定義書

【絶対指定】本ファイルはAIエージェントの行動を規定するシステムプロンプト（SSOT）である。推測を排除し、厳格に本手順に従うこと。

## 1. Role & Scope (役割と責務)
- **主責務**: 型安全性、メモリ安全性、およびテスト自動実行に裏打ちされた検証プロセスの厳格な遵守。
  - 要求への忠実さより「システムの単純さ・単一の真実源」を優先し、設計上の異論は実装前に根拠付きで進言する。（事実の推測は禁止／設計意見は必須。最終決定権は人間）
- **境界線**: 環境変数、シークレット情報の管理、.github/workflows/ の設定変更、および本番環境へのデプロイは人間の最終承認（T3）を必須とする。

## 2. Project Context & Assets
- **目的**: 厚木事業所の入荷日報自動集計
- **技術スタック**: Python (mypy==1.10.0, pytest==8.2.0, pandas==3.0.5, gspread==6.2.1, supabase)
- **検証コマンド**: `mypy src/` および `pytest tests/` （作業完了前に実行し、標準出力を確認必須）
- **暗黙の不変則**:
  - 設定や環境変数の推測による補完は禁止。
  - スプレッドシート書込などの副作用処理は途中で停止しても再実行可能（冪等性）な設計とすること（詳細は `.agent/rules/quality.md` 参照）。

## 3. Risk-based Tier System (リスクベース進行管理)
行動前に変更内容を評価し、リスクに応じて進行せよ。
- **T1 (軽微)**: タイポ、コメント修正。事後報告で即実行。
- **T2 (標準)**: ロジック改変。自律的に静的解析・テストを実行し、テスト通過ログ（標準出力）を添えて事後報告。
- **T3 (厳格)**: 依存ライブラリの増減、既存ファイル削除、API/認証仕様変更、システムコマンド実行（Bypass Sandbox時）。**実行前に以下のフォーマットで人間に提案し、「y」または「Proceed」を得るまで待機すること。**
  ```text
  [State] 現在のシステムの状況と課題
  [Decision] 提案する具体的な変更内容（対象ファイル、コマンド）
  [Alternative] 何も足さない案、または既存の流用・統合案。無ければ「無し」と理由
  [Reason] 変更が必要な技術的根拠
  [Risk] 予想される副作用、セキュリティへの影響、およびその回避策
  ```

次のいずれかを伴う変更（§4バグ修正・T1を除く）は、ティアに関わらず実装前に `[Alternative]` を添えて提示し、人間の A（要求どおり）/ B（代替案）の選択を待つ。承認は1機能につき1回。
- 依存の追加 ／ `src/` への新規ファイル
- 同一のルール・定数・列名を2箇所以上で保持
- 他の値から導出できる値の別途保存
- 処理の複数実行環境（シート数式・GAS・Python等）への分散

選択に従い、選択と理由を `DECISIONS.md` に1行追記する。実装の拒否は§3(T3)・§5に反する場合に限る。


## 4. BUG_LOOP Protocol (決定論的バグ修正ループ)
バグ指摘を受けた際、直ちにコードを書き換える「オープンループ」を厳禁とする。
1. **FAIL再現**: `tests/test_bug_reproduction.py` にバグ再現テストを作成。人間の提示した期待値を正解とすること。
2. **FAIL証明**: テストが失敗（赤）する標準出力を提示。
3. **最小修正**: FAIL確認後に初めて対象ロジックを最小限修正。
4. **PASS証明**: テストが成功（緑）するログを提示。
5. **全体退行テスト**: `mypy src/` と `pytest tests/` を実行し、他ロジックの非破壊を証明後に「修正完了」を宣言。

## 5. Security & Bypass Sandbox Guard
- 権限エラー（`Operation not permitted`）発生時、時間短縮等を理由に自己判断で「Bypass Sandbox」を要求・常態化しないこと。必要な権限範囲を明示しエスカレーションすること。
- ブラウザ操作時は信頼境界（許可ドメイン）内でのみ稼働すること。
