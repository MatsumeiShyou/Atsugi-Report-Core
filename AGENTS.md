# 厚木事業所_入荷日報自動化_AGENTS定義書
【絶対指定】本ファイルはAIエージェントの行動を規定するシステムプロンプト（SSOT）である。推測を排除し、厳格に本手順に従うこと。

1. システムと権限の境界
- 目的: 厚木事業所の入荷日報自動集計。
- 権限制約: 環境変数および .github/workflows/ の設定変更はT3（要人間承認）とする。推測での変数補完は禁止。

2. 開発・検証コマンド
AIは作業完了前に必ず以下のコマンドを実行し、標準出力を確認すること。
- 型検査: mypy src/ (必須)
- テスト: pytest tests/ (必須)

3. リスクベース進行管理 (Tier System)
行動前に変更内容を評価し、指定フォーマットで人間に報告すること。
- T1 (軽微): コメント修正、タイポ修正。事後報告でよい。
- T2 (標準): ロジック変更。必ず pytest をパスした標準出力を添えて報告すること。
- T3 (厳格): API認証変更、ファイル削除、外部ライブラリ追加。事前に [State] [Decision] [Reason] [Risk] のフォーマットで人間に承認（y）を求めること。

4. ユーザー発見バグの修正ループ (BUG_LOOP)
人間から指摘を受けた場合、直ちにコードを修正してはならない。
- 検証条件のコード化: 異常を再現するテストコードを tests/test_bug_reproduction.py に作成する。
- FAILの証明: テストが失敗（FAIL）することを確認し提示する。
- 修正とPASSの証明: コード修正後、テストが成功（PASS）した標準出力を提示する。
- 完了宣言: 他の全テストもPASSしたことを確認した後、初めて「修正完了」と宣言する。
