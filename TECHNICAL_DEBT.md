# 技術的負債 (Technical Debt)

本ファイルは、将来的に解決すべきシステムの課題や過剰設計、運用上の負債を記録する。

## 1. 【データ負債】未登録品名（マッピング定義）の不足
- **内容**: 現場の運用にのみ存在し、システム（`mapping_definitions.py`等）が認識していない品名や取引先名が大量に存在している（例: 8月の明細で約80万kg分の未登録品名）。
- **影響**: 入荷実績の数字が人間の作成版と大きく乖離する（集計漏れが発生する）。
- **対応方針**: 人間が作成した日報と照らし合わせ、不足している品名・取引先名のリストをシステムのマッピング定義に継続的に追加・更新する運用フローを確立する。

## 2. 【設計負債】プレゼンテーション層 (`excel_presenter.py`) の過剰設計
- **内容**: エージェントが「動的数式（=1330+1730等）を維持する」「固定の雛形を読み込んで書き込む」という要件をすべて満たそうとした結果、`excel_presenter.py` のコードが極めて複雑になった。
- **影響**: プログラムが複雑になりすぎたことで、「集計シートの末尾に総合計を出す」といった基本的な出力が欠落するデグレ（劣化）が発生している。
- **対応方針**: 「人間がエクセルで手作業で行っていた見た目や数式」にシステムを無理に合わせるのではなく、早期に「Pythonで計算済みの静的数値を単純に吐き出すだけのシンプルな設計」へ引き算（リファクタリング）する。
